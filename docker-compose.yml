services:
  jie-email:
    build:
      context: .
    container_name: jie-email
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      TEMP_MAIL_BASE_URL: ${TEMP_MAIL_BASE_URL}
      TEMP_MAIL_ADMIN_AUTH: ${TEMP_MAIL_ADMIN_AUTH}
      TEMP_MAIL_SITE_AUTH: ${TEMP_MAIL_SITE_AUTH:-}
